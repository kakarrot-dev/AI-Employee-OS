# AI Employee OS 架构总览

> 现行入口文档。与根目录 `AGENTS.md` 对齐；冲突时按 AGENTS 事实源优先级裁决。本文不定义 Schema 或接口字段。

## 产品定位

Local-first 的 macOS AI 员工操作系统：客户端负责交互与 Keychain，Rust Runtime 负责状态、权限与 Tool 执行，Python Worker 负责意图、闲聊与规划推理。

当前主验证路径：DeepSeek Key → 编辑员工 Identity / Soul / Persona → 多轮对话 → 意图路由；工作意图按员工已绑定 Skill 的 readiness 进入 Resolver → Generic Run Kernel → ToolExecutor → verified Deliverable。跨员工闭环以数据搜集员工的 `web-search` 结果经 verified Handoff 交给只具备 `local-file-operations` 的文档编写员工，最终形成审批过的 Markdown Artifact。

## 进程边界

```text
Swift macOS Client
  UI、本地 Store、Keychain；不推理、不直接执行 Tool
        |
        v
Thin Rust Runtime
  Task/Action、权限、审批、Tool Gateway、SQLite、事件
        |
        v
Python Agent Worker
  意图分类、闲聊、Context/规划、LLM Provider；不直接取得系统权限
```

硬规则：

- 所有 Tool 调用必须经过 Rust `ToolExecutor`。
- Secret 只经 Keychain 注入受控进程环境；不进入仓库、SQLite、日志、Trace、Memory 或 Agent Context。
- Skill 经 `agent_skills` 绑定到员工；Tool 全局安装（无 per-agent Tool 绑定表）；客户端不创建 Package。
- 进程间通信为 CLI / 子进程 JSON，不是 gRPC。
- 默认工作编排为通用 bounded agent loop，可选 workflow；Rust 是 Task/Run/Action 状态与副作用事实源（ADR-032）。LangGraph / Deep Agents 不是状态源。

## 产品导航

七模块：办公室、通讯录、工作库、知识库、技能库、工具库、设置。知识库当前只读展示 Runtime SQLite 中的 canonical Knowledge 来源与索引状态；本地文件只有经 Runtime 导入后才会进入该页面。页面级事实源见 [Main Interface Spec v2.0](./design-system/AI%20Employee%20macOS%20Main%20Interface%20Spec%20v2.0.md)；视觉 Token 见 [Claude Cream](./design-system/Claude%20Cream%20macOS%20UI.md)。

## MVP 分层

| 层 | 内容 |
|---|---|
| 产品可见 | 多轮对话、员工 Profile、仓库 Package 安装与列表、Skill 绑定、办公室 Task Proposal、Task Thread 双员工执行与最终产物 |
| Runtime 已有、产品未全暴露 | Task/Run/Action、Snapshot、Checkpoint、Permission、Approval、Audit、Artifact/Deliverable、Memory/Knowledge/Evaluation |
| 暂不包含 | Computer Use、并行 Multi-Agent、Task Thread 外自由群聊、Cloud Sync、Marketplace、企业 RBAC、任意网页抓取、公证发行 |

客户端已支持多员工 Profile 的创建、编辑与停用；数据搜集员工与文档编写员工的串行协作是当前多员工主验证路径。

## 事实源

1. [Unified Data Model](./AI%20Employee%20OS%20Unified%20Data%20Model%20v1.0.md)
2. `contracts/`
3. [MVP API & Interface Specification](./AI%20Employee%20OS%20MVP%20API%20&%20Interface%20Specification%20v1.0.md)
4. 领域专题：Security、Runtime、Memory、Skill、Tool、Observability、[PM Agent 规范](./AI%20Product%20Manager%20Agent%20实现规范%20v1.0.md)
5. [ADR](./AI%20Employee%20OS%20技术决策记录%20ADR（Architecture%20Decision%20Records）v1.0.md)

验证门禁：`./scripts/check.sh`。本地打包启动：`./script/build_and_run.sh`。
# 多员工业务流内核（产品入口已收敛）

客户端不再暴露「场景库」及手工 Scenario 创建、编辑、启动入口。既有 Scenario Definition、Business Flow、Root/Child Task、WorkOrder、Handoff、Root 输出绑定和历史数据继续保留，用于内部物化、只读投影、恢复与统一任务入口复用；不得删除 Migration、Audit、Artifact 或已确认副作用。办公室自然语言入口已通过模型生成并由 Runtime 校验单员工或多员工 Task Proposal，确认后物化 Task Thread；工作库展示参与员工、WorkOrder、审批、Handoff、进度与最终产物。
