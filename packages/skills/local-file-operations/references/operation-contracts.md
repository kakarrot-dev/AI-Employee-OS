# 操作契约

## 读取

提供明确路径，调用 `read_file`。返回真实文件内容；不存在或不可读时保留原始错误语义。

## 创建

提供明确路径与完整内容，调用 `create_file`。目标已存在时返回冲突，不覆盖。

## 编辑

提供明确路径、唯一旧文本与新文本，调用 `edit_file`。旧文本不存在或出现多次时返回冲突，不猜测替换范围。

每次执行只调用一个与当前操作匹配的 Tool action。信息不足时先询问用户。
