# 数据源路由

Agent Reach 可检测多个公开网络数据源及其后端状态，但当前员工可调用的固定 action 为 `agent-reach-tool.search_web`。

- 优先使用已就绪且已暴露给 ToolExecutor 的后端。
- 数据源“已配置”不等于“已暴露给员工”，也不等于搜索结果已经核验。
- 来源不足时说明限制，不跨平台补造内容。
- 工具库中的状态来自运行时诊断快照，应结合检查时间判断新鲜度。
