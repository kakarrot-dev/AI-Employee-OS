# 认证边界

- Cookie、Token、API Key 与浏览器凭据不得进入仓库、SQLite、日志、Trace、Memory 或模型上下文。
- 客户端只展示凭据类型与存在状态，不展示原文。
- 浏览器会话由用户在受控浏览器中登录并授权；Runtime 不扫描或导出浏览器 Cookie 数据库。
- 需要登录或扩展连接时返回明确提示，由用户完成交互后重新诊断。
- 诊断成功只表示数据源可访问，不代表该数据源已通过 ToolExecutor 暴露给员工。
